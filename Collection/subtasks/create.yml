info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseDEV}}/subtasks"
  body:
    type: json
    data: |-
      {
      "task_id": "cmp2yzber00007byigrgqr9l3",
      "title": "Crear validaciones",
      "order": 2,
      "is_completed": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
