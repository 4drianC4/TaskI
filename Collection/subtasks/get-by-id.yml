info:
  name: get-by-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseDEV}}/subtasks/:id"
  params:
    - name: id
      value: cmp4bif5v00029u53fxga463w
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
