info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseDEV}}/tasks"
  body:
    type: json
    data: |-
      {
        "column_id": "column-1",
        "title": "Nueva tarea",
        "description": "Descripción de prueba",
        "order": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
