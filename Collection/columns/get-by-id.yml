info:
  name: get-by-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseDEV}}/columns/:id"
  params:
    - name: id
      value: column-1
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
