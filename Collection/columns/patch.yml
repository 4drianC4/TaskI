info:
  name: patch
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseDEV}}/columns/:id"
  params:
    - name: id
      value: bca329eb-177c-427e-a531-21b4d61d4177
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Columna actualizada con patch"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
