info:
  name: get-all
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseDEV}}/columns?boardId=board-1"
  params:
    - name: boardId
      value: board-1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
